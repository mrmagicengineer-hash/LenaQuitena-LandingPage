FROM node:22-alpine
WORKDIR /app

# Native deps for better-sqlite3
RUN apk add --no-cache python3 make g++

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .
RUN npx prisma generate && npm run build:server

EXPOSE 3001
CMD ["sh", "-c", "npx prisma db push && npm run start:server"]
